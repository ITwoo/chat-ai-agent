{
    "name": "analysis-service",
    "private": true,
    "scripts": {
        "dev": "uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8001",
        "test": "uv run python -m pytest"
    }
}